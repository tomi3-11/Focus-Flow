# Focus Flow
A simple Flask web app that allows users to add tasks, check calendar and use focus sessions.

```
flask crud application with authentication, Using blueprints, using MySql as db. all the configurations

```